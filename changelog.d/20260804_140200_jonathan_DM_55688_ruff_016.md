### Other changes

- Move to ruff 0.16. The vendored `ruff-shared.toml` is re-synced with the `lsst/templates` `fastapi_safir_app` template to add `CPY001` (`missing-copyright-notice`) to the ignore list, since ruff 0.16 stabilized that rule out of preview and it would otherwise fire on every source file. The `ruff` pin in the `lint` dependency group moves to `>=0.16,<0.17` to stay in sync with the `ruff-pre-commit` rev.
